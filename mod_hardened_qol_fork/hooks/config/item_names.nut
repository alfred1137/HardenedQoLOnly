// Vanilla Adjustments
::MSU.Array.removeValues(::Const.Strings.BillNames, [
	"Reach",	// This is re-used multiple times. We restrict it to just Bows
]);

::MSU.Array.removeValues(::Const.Strings.CrossbowNames, [
	"Reach",	// This is re-used multiple times. We restrict it to just Bows
]);

::MSU.Array.removeValues(::Const.Strings.WhipNames, [
	"Reach",	// This is re-used multiple times. We restrict it to just Bows
]);
